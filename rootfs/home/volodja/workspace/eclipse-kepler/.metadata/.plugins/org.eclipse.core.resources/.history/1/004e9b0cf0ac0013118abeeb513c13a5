/*
 * main.cpp
 *
 *  Created on: 15 марта 2014 г.
 *      Author: volodja
 */




#include <iostream>
#include <string>
#include <typeinfo>
#include <algorithm>
#include <vector>


using namespace std;


void func(const string &s) {
    cout << s << endl;
}


class A {
public:
    A(const string &&name)
    :
        name(name)
    {
        cout << "create  object   name " << name << endl;
    }

    A(const A &a)
    :
        name(a.name)
    {
        cout << "copy    object   name " << name << endl;
    }

//    A(A &&a)
//    :
//        name(a.name + " copy")
//    {
//        cout << "move    object   name " << a.name << endl;
//    }

   ~A() {
        cout << "destroy object   name " << name << endl;
    }

private:
    string name;
};


int main() {
    vector<A> v;
    v.emplace_back(A("a"));
//    A a = move(A("name"));
//    A b = a;
//    v(move(A("name1")));

//    func (
//        []() -> string {
//            string result = "qwert";
//            cout << "lambda: " << result << endl;
//            return std::move(result);
//        }());

    return 0;
}
